<?xml version="1.0" encoding="utf-8"?>
<launch>
    <arg name="rviz_en" default="true" />

    <node pkg="traj_smoother" type="traj_smoother_node" name="traj_smoother" clear_params="true" output="screen">
        <rosparam command="load" file="$(find traj_smoother)/config/traj_smoother.yaml" />
    </node>

    <!-- <group if="$(arg rviz_en)">
        <node pkg="rviz" type="rviz" name="rviz" args="-d $(find traj_smoother)/rviz/traj_smoother.rviz" output="screen" />
    </group> -->

</launch>